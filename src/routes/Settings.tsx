import React, { useState } from 'react';
import { Layout, Button, Modal, Select, Row, Col } from 'antd';
import { BgColorsOutlined, FontSizeOutlined, FontColorsOutlined, SmileOutlined, SkinOutlined } from '@ant-design/icons';

const { Header, Content } = Layout;
const { Option } = Select;

const Settings: React.FC = () => {
    return (
        <h1>Setting</h1>
    );
};

export default Settings;
